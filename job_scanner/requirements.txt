# Weekly-run dependencies. Deliberately small, and deliberately without a
# browser: Playwright is a discovery-time tool only (requirements-browser.txt)
# and never ships in the scheduled scan.
requests>=2.31
PyYAML>=6.0
beautifulsoup4>=4.12
lxml>=5.0
openpyxl>=3.1
python-docx>=1.1
