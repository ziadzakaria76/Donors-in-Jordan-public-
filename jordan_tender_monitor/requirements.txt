# HTTP and HTML
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0            # the XML parser the RSS layer needs
tenacity>=8.2.0        # retry with exponential backoff

# Parsing
python-dateutil>=2.8.2

# Ranking
rapidfuzz>=3.5.0       # cross-portal duplicate detection

# Output formats
openpyxl>=3.1.0        # Excel
python-docx>=1.1.0     # Word bid-review pack

# Configuration and delivery
python-dotenv>=1.0.0
msal>=1.25.0           # Microsoft Graph authentication

# The IANA time zone database.
#
# Required on Windows and NOT optional there. Linux and macOS ship a system tz
# database that zoneinfo reads from /usr/share/zoneinfo; Windows ships none, so
# ZoneInfo("Asia/Amman") raises ZoneInfoNotFoundError and --schedule dies on
# startup. The schedule is pinned to Asia/Amman deliberately (Jordan is UTC+3
# year-round), so this is load-bearing on a Windows host.
tzdata>=2024.1; platform_system == "Windows"

# Deliberately NOT here: playwright. Exactly one portal needs it (UNGM, whose
# listing is assembled client-side), and it pulls ~400 MB of browser. Putting it
# in this file would make a Windows deployment that never touches UNGM pay for
# it. It lives in requirements-browser.txt instead:
#
#     pip install -r requirements-browser.txt
#     playwright install chromium
#
# Without it UNGM reports "unavailable" with that instruction as its reason, and
# the other twelve portals are unaffected.
