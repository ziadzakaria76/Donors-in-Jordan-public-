<?xml version="1.0" encoding="utf-8"?>
<!-- The dark counterpart. Same style name, dark platform parent, so the window
     is already dark before Compose draws its first frame. -->
<resources>

    <style name="Theme.Gs3MarketingOps" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

</resources>
